'use client';

// BotanicalBackground is intentionally empty — the editorial redesign uses
// solid colour section backgrounds instead of a global fixed image layer.
const BotanicalBackground = () => null;

export default BotanicalBackground;
